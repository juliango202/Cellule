FIND_PACKAGE(Qt4 REQUIRED)

INCLUDE_DIRECTORIES(include ${QT_INCLUDE_DIR})

FILE(GLOB HEADERS "include/*.h")

QT_WRAP_CPP(cellule MOCS ${HEADERS})

FILE(GLOB SOURCES "src/*.cpp")

ADD_LIBRARY(base STATIC ${SOURCES} ${MOCS})
